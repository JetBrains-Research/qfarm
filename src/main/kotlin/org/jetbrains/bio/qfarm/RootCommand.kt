package org.jetbrains.bio.qfarm

import com.github.ajalt.clikt.core.CliktCommand

class RootCommand : CliktCommand() {
    override fun run() {
        // optional: runs if no subcommand is provided
    }
}
